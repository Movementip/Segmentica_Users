// Заготовка под явные контракты и интерфейсы.
// Если тип используется только внутри frontend, его лучше положить в frontend/types.
// Если тип общий для нескольких частей проекта, ему место в корневом types/.
